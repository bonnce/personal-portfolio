---
import Layout from "../../layouts/Layout.astro";
import { projects } from "../../utils/projects_lists";

export function getStaticPaths () {
 return projects.map(project => ({params: {id:project.id}}));
}

const {id} = Astro.params

const project = projects.filter(pro => pro.id === id)[0]
---
<Layout title={`Project-${id}`}>
  <section class="flex flex-col items-center w-full h-full overflow-y-auto overflow-x-hidden relative">
    <div class="relative left-0 top-0 w-full h-1/3 overflow-hidden">
      <img class=" w-full h-full object-cover z-0 blur-sm brightness-75" src={project.img} alt={project.alt}>
      <span></span>
    </div>
    <h2 class="absolute top-[10vh] text-5xl text-green-200 z-10 uppercase font-space-mono ">{project.title}</h2>
    <p class="text-green-500 text-2xl md:w-3/4 mt-5">
    Lorem ipsum dolor sit amet consectetur adipisicing elit. Iure saepe cumque exercitationem ex dolor iusto reprehenderit non? Iste consectetur vitae culpa veniam, qui numquam, cupiditate consequuntur doloribus eum debitis quis.
    Lorem ipsum dolor sit amet consectetur adipisicing elit. Minima ullam magnam molestiae velit excepturi odio nemo sunt molestias minus, possimus quo ipsa. Earum incidunt iure, quasi exercitationem veniam facere quisquam!
    </p>  
  </section>
</Layout>

<style>
  span{
    position: absolute;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    background: var(--bg-body);
    background: linear-gradient(0deg, var(--bg-body) 10%, transparent 100%); 
  }
</style>